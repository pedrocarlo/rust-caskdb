mod format;

use std::path::PathBuf;

struct Database;

#[derive(Debug)]
struct Connection {
    // dir: String,
    // active_file: PathBuf,
}

impl Connection {
    pub fn new() -> Self {
        Self {}
    }

    
}
